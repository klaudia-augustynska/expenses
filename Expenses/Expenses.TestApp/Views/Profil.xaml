<UserControl x:Class="Expenses.TestApp.Views.Profil"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Expenses.TestApp.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Style="{StaticResource mainGrid}">

        <StackPanel>
            <TextBlock Style="{StaticResource h1}">Profil</TextBlock>

            <Button Command="{Binding UsunKonto}"
                    Style="{StaticResource field}">Usuń konto</Button>
        </StackPanel>
        
    </Grid>
</UserControl>
